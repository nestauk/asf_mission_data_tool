#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "asf_mission_data_tool")"
[ "$CONDA_DEFAULT_ENV" = "asf_mission_data_tool" ] || echo "Have you run \`make install\`?"
